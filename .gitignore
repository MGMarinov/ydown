# local runtime and generated data
tmp_debug/
tmp_runtime/

# local binaries (installed via install-tools scripts)
bin/ffmpeg/

# private internal development docs (must never be pushed public)
docs/
